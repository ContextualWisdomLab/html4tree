# 변경 사항 (CHANGELOG)

## [Unreleased]
### 추가됨 (Added)
- 생성되는 HTML 문서의 반응성을 향상시키기 위해 `<head>`에 `<meta name="viewport" content="width=device-width, initial-scale=1">` 태그 추가
- 문자 인코딩 명시를 위한 `<meta charset="utf-8">` 태그 추가
- 스크린 리더 및 브라우저 호환성을 개선하기 위해 `<html>` 태그에 `lang="en"` 속성 추가
- 핵심 비즈니스 로직(`main.kt`, `util.kt`)에 대한 단위 테스트 추가 (코드 커버리지 100% 달성)
