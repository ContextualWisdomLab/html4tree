# 변경 사항

## [Unreleased]
### 추가됨
- 생성된 HTML 디렉토리 트리의 가독성을 개선하기 위해 시스템 폰트, 반응형 최대 너비 설정, 그리고 다크 모드(`@media (prefers-color-scheme: dark)`) 지원을 추가했습니다. (UX 개선)

### 수정됨
- 최상위 디렉토리가 심볼릭 링크인 경우를 올바르게 거부하도록 검증 로직을 수정했습니다. 이전에 `canonicalFile` 호출로 인해 발생하는 검증 우회 버그를 해결했습니다.
